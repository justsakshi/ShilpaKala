<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <!-- Camera preview fills screen -->
    <androidx.camera.view.PreviewView
        android:id="@+id/previewView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Overlay on top of preview -->
    <com.example.shilpakala.OverlayView
        android:id="@+id/overlayView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <ImageView
        android:id="@+id/appLogo"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:src="@drawable/logo"
        android:contentDescription="Shilpa-Kala logo"
        android:layout_marginTop="44dp"
        android:layout_marginStart="20dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- App title — pushed down below the punch-hole camera -->
    <TextView
        android:id="@+id/appTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Shilpa-Kala"
        android:textColor="#F5F0E8"
        android:textSize="20sp"
        android:fontFamily="serif"
        android:textStyle="bold"
        android:letterSpacing="0.05"
        android:layout_marginTop="52dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Bottom bar: language toggle LEFT, capture button CENTRE -->
    <LinearLayout
        android:id="@+id/bottomBar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="28dp"
        android:paddingEnd="28dp"
        android:paddingBottom="36dp"
        android:paddingTop="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Language toggle pill — left, easy thumb reach -->
        <Button
            android:id="@+id/langToggleBtn"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:text="ಕನ್ನಡ"
            android:textSize="15sp"
            android:textColor="#F5F0E8"
            android:backgroundTint="#80000000"
            android:singleLine="true"
            app:cornerRadius="24dp" />

        <!-- Gap -->
        <View
            android:layout_width="20dp"
            android:layout_height="1dp" />

        <!-- Capture button — wide enough for "Capture" on one line -->
        <Button
            android:id="@+id/captureBtn"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:text="Capture"
            android:textSize="15sp"
            android:textColor="#1A1209"
            android:backgroundTint="#C9A84C"
            android:singleLine="true"
            android:ellipsize="none"
            app:cornerRadius="60dp" />

        <!-- Right spacer mirrors left weight so circle stays centred -->
        <View
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>